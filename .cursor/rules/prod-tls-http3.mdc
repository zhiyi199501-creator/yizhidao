---
description: 生产 TLS（国内直连 iPhone 11；安卓勿用 HttpURLConnection）
alwaysApply: true
---

# 生产 TLS / 国内直连

iOS / Android **Release 现役**：`https://api.yiwanjia.work`（新加坡 `124.156.192.137`，**H2-only**；App **仅海外上架**）。同机备用 / 对照：`v1.yiwanjia.work`、`yizhidao.codedance.work`、`yd.codedance.work`（均 H2-only，App Release **不**改指）。

**2026-09-04 纠正（覆盖「整段 IP 全灭 / 名字被拉黑」）**：iPhone 11 无代理时常见 TLS 握手中途 RST → `-1200`；开代理三域都通。同机同栈关代理时 **仅** `yizhidao.codedance.work` 直连通，`api` / `yd` 不通 → **不是**证书坏、**不是**整段 `124.156.192.137:443` 对所有主机名封死、**也不是**「iPhone 把域名永久拉黑」（`yizhidao` 早期用过且当时不通，现役 H2 指新加坡后关代理可通）。更像按主机名的路径差异（SNI / 本机粘连等）。iPhone 17、安卓 Cronet 直连海外通常正常。

## 禁止

- 为国内 iPhone 11 直连去改 App Release 基址、轮换子域/换证，或上 iOS Cronet / 自建 TLS「换指纹」。开代理能通的名字没有废；产品排除大陆，海外直连是验收标准。
- Cloudflare **橙云** / 任何 CDN 代理生产 API 域（边缘会再开 h3、发 `Alt-Svc`、换证书链）。解析只 DNS-only（灰云），A 直指源站。
- 只在 Mac / iPhone 17 上验收「国内直连是否健康」；也勿用国内 iPhone 11 无代理失败否定海外直连。
- 让国内真机把 DNS 改成 `8.8.8.8`（常被墙，整网超时）。
- 安卓禁止用 `HttpURLConnection` 打生产域名；禁止 `addQuicHint` 指生产域。现役走 Cronet。
- 用 `:80` 通配反代抢走 ACME HTTP-01。公网 IP 的 HTTP 请写 `http://43.128.104.104`。

## 要做

- 现役钉 `api.yiwanjia.work`，H2-only（`protocols h1 h2`，不发 `Alt-Svc`）。新名字也从第一天 H2-only。
- iOS：`URLSession` ephemeral + `assumesHTTP3Capable = false`（防系统粘住旧 h3）；救不了握手中途 RST。
- **国内 iPhone 11 测生产**：代理 / TUN 全局，API 域名走代理（勿智能分流直连）。对照可开 Safari：`api` / `yizhidao` / `yd` 的 `/health`。
- 海外用户、审核直连即可。Debug 真机走局域网 IP；Release Build Variant 才打生产 HTTPS。

细节见 `docs/deploy.md`「DNS」「iPhone 11 / 国内直连」和「Android / Cronet」。
